// Funções em JS


// função padrão

function somar(a, b) {
    return a + b;
}

// palavra chave function para declarar a função
// palavra chave return para retornar um valor


// JSDoc, para documentar funções, explicações, etc


/**
 * 
 * @param {String} email 
 * @param {String} password 
 * @returns {Boolean} confirmação de login
 */
function signIn(email, password) {

    // código super complexo

    return true;
}

// quando se usar: signIn() a IDE vai mostrar os parâmetros que a função espera

/**
 * 
 * @param {Number} a 
 * @param {Number} b 
 * @returns {Number}
 */
function somar(a, b) {
    return a + b;
}

